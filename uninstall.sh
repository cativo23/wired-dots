#!/usr/bin/env bash
# wired-dots — uninstall entry point
# v1.0: removes repo symlinks from ~/.config/* and ~/.local/bin/*,
#       restores latest backup from ~/.config/cfg_backups/wired-*/
# v1.5: full rollback (packages, services, bootloader)

set -euo pipefail

echo "wired-dots uninstall.sh is not implemented in this milestone (M0)."
echo "Planned for M4 (minimum viable) — v1.5 (full)."
exit 1
